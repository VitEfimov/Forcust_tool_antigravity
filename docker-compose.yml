version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1

  # Frontend service will be added after we create it
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:80"
